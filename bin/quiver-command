#!/usr/bin/env node

'use strict'

var command = require('../lib/command')
var commandArgs = require('optimist').argv

command.runCommand(commandArgs, process, function(err) {
  if(err) throw err

  process.exit(0)
})